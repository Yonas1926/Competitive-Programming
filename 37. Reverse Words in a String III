class Solution:
    def reverseWords(self, s: str) -> str:
        
        s = s.split()
        ans = []
        
        for i in s:
            x = i[::-1]
            ans.append(x)
        
        return " ".join(ans)
